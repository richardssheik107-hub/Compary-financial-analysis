# 2026-05-24 Step56 工作日志（AKShare 稳定性与提示优化）

## 时间线

### 1) 问题定位
- 动作：检查 `src/data_service.py`、`app.py` 的 AKShare 调用和 warning 展示路径。
- 产出：确认当前是“单次失败即回退”的机制，缺少针对临时网络抖动的重试策略。
- 关键定位：
  - `src/data_service.py::_fetch_akshare_metrics_with_retry`
  - `src/data_service.py::_fetch_akshare_daily_prices_with_retry`
  - `app.py::_render_analysis`

### 2) 连通性复测
- 动作：本地脚本直连 AKShare 接口验证。
- 产出：接口在当下可用（说明失败更可能是瞬时故障，而非永久不可用）。
- 结果：
  - `akshare==1.18.60` 导入成功；
  - `stock_info_a_code_name` 成功；
  - `stock_financial_abstract('600519')` 成功；
  - K 线三套接口均成功。

### 3) 后端重试增强
- 动作：在数据层加入“指数退避 + 临时错误识别”。
- 产出：
  - 新增 `_is_transient_akshare_error(exc)`；
  - metrics 和 daily 两条 AKShare 调用路径统一支持 3 次重试；
  - 退避时长 `0.8s -> 1.6s -> 3.2s`；
  - 保留代理自动关闭兜底机制。
- 决策原因：AKShare 上游偶发波动常见，重试可显著降低“假失败”率。

### 4) 前端提示优化
- 动作：在 `app.py` 对 warning 做用户可读化包装（`_friendly_data_warning`）。
- 产出：当 live 数据失败时，明确告诉用户“已切换兜底，页面可继续使用”；K 线失败时说明“不影响文本分析”。
- 决策原因：降低用户对“失败”字样的焦虑，提升可用性理解。

### 5) 回归与编译检查
- 动作：
  - `python -m py_compile app.py src/data_service.py`
  - `build_company_snapshot('600519')` 烟测
- 结果：通过；`600519` 可识别并返回行情点位。
- 备注：发现中文公司名在当前环境存在编码不稳定，暂不在本步修复，后续单开一步处理。

## 本步可测试状态
- 已达到可测试：是
- 推荐测试输入：`600519`
- 预期：
  1. 页面能输出完整分析；
  2. 若 AKShare 瞬时失败，warning 显示“已自动切换兜底数据”的说明；
  3. 若仅 K 线失败，提示“不影响文本分析”。
