# 2026-05-24 Step57 工作日志（上下文追问增强）

## 时间线记录

### 1) 现状检查
- 动作：检查 `app.py`、`src/agent.py`、`src/research_agent.py` 的上下文链路。
- 发现：
  - 单公司追问已有补全能力；
  - 两公司对比后，用户省略公司名追问时，主路由偶发退化为通用问题；
  - 深度研究报告使用原始 query，可能与主路由补全 query 不一致。

### 2) 实施改动
- 文件：`app.py`
- 变更 A（对比追问补全）：
  - 在 `_run_query` 增加 `resolved_query` 生成逻辑。
  - 条件：已有 `compare_pair`、本次输入未显式公司名/代码、且包含对比信号词。
  - 产出：自动拼接为“公司A和公司B + 当前问题”。
- 变更 B（报告口径一致）：
  - `run_company_research_agent(...)` 入参改为 `agent_result.resolved_query or query`。

### 3) 验证
- 动作：`python -m py_compile app.py src/agent.py src/research_agent.py`
- 结果：通过。
- 关键检查：`resolved_query`、`compare_signals`、研究报告入参替换均已落地。

## 决策说明（为什么这样做）
- 不改后端主流程、只在入口做轻量补全，能最小风险提升追问可用性。
- 主分析和研究报告统一使用同一个“实际解析问题”，可避免用户感知到前后矛盾。

## 可测试状态
- 已可测试：是
- 推荐用例：
  1. `贵州茅台和宁德时代谁更好`
  2. `再对比一下现金流质量`
  3. `哪个未来更稳`
- 预期：2/3 不写公司名也会继续两公司对比，而非退化为通用问答。
