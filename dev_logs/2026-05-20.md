# 2026-05-20 开发日志

## 今日目标

完成 Step 1：建立 Agent 化基线与文档日志体系，做到“可运行、可阅读、可交接、可测试”。

## 时间线记录

### 11:35 - 11:45 执行动作

- 阅读现有项目结构、数据层与 AI 层实现，确认不破坏原有功能的接入点。
- 检查历史日志格式，决定沿用现有风格并增加“时间线 + 决策理由”。

产出：

- 确定新增 `src/agent.py` 作为编排入口。
- 确定扩展 `src/models.py` 增加 Agent 结构体。

决策与理由：

- 选择“最小侵入式接入”，避免一次改动过大影响稳定性。

---

### 11:45 - 12:05 执行动作

- 新增 `src/agent.py`，实现 `run_financial_agent(query)`。
- 增加最小 SOP：意图识别 -> 公司识别与数据抓取 -> 财报解读生成。
- 扩展 `src/models.py`，增加 `AgentStep` / `AgentResult` 数据结构。

产出：

- 可复用的 Agent 编排入口已落地。

决策与理由：

- 意图识别采用规则法，优先满足演示可解释性，后续再升级模型判别。

---

### 12:05 - 12:20 执行动作

- 修改 `app.py` 查询入口，统一改为调用 `run_financial_agent(query)`。
- 新增“Agent 执行过程”展示区，逐步展示执行步骤与状态。
- 保留原有分析结果区，不改变核心业务输出。

产出：

- 前端可见 Agent trace，且原财报结果渲染仍可复用。

决策与理由：

- 保留原结果区可减少回归风险，确保 Step 1 主要验证“可编排 + 可展示”能力。

---

### 12:20 - 12:35 执行动作

- 编写 Step 1 阶段说明文档：`docs/step-01-agent-baseline.md`。
- 写入目标、现状、SOP、设计决策、验收标准、测试指引、风险与下一步。

产出：

- 阶段文档可直接用于项目交接和求职讲述。

决策与理由：

- 文档以“行为和结果”为主，不做泛化描述，确保他人可复现实操。

## 本步完成项

- 完成最小 Agent 编排能力接入。
- 页面新增 Agent 执行过程展示。
- 建立 Step 1 阶段文档与当日日志双轨记录。
- 完成 Step 1 验证：编译通过、服务健康检查通过、两条查询链路通过。

## 当前待办

- 输出测试清单并提醒用户开始测试。
- 后续继续迭代 ToolRegistry 与更细粒度 trace。

## 风险与阻塞

- 意图识别仍是规则匹配，复杂语句下可能误判为通用问答。
- 当前 trace 仅展示步骤摘要，缺少更细粒度工具入参与结果片段。

## 下一步建议

- Step 2 增加 ToolRegistry 与步骤级质量评估。
- 针对离线演示和真实 API 输出分别补充验收截图。

## 本次追加（验证记录）

### 验证动作

- 运行 `python -m compileall app.py src`：通过。
- 检查 `http://localhost:8502/_stcore/health`：返回 `ok`。
- 执行函数级冒烟：
  - `run_financial_agent("贵州茅台")` -> `found=True`，步骤数=3
  - `run_financial_agent("帮我看看000001")` -> `found=True`，识别为平安银行（000001），步骤数=3
- 无 API Key 场景确认：`ai_status` 返回本地兜底信息。

### 验证结论

- Step 1 已达到“可开始测试”标准，可通知用户进行页面测试。

## 本次追加（Step 2）

### 12:50 - 13:20 执行动作

- 实现 `ToolRegistry` 轻量调度层，统一编排数据抓取和 AI 解读工具调用。
- 扩展 Agent 数据结构，新增工具调用记录与输出质量评估结构体。
- 前端新增“工具调用明细”和“输出质量评估”展示区。
- 新增 Step 2 阶段文档 `docs/step-02-tool-registry-evaluation.md`。

产出：

- Agent trace 从“步骤级”升级为“步骤级 + 工具级 + 质量评分”。

决策与理由：

- 先上规则评分，确保稳定可解释；后续再引入更复杂评估策略。

### 当前待办

- 运行编译与函数级冒烟验证。
- 通过后通知用户进行 Step 2 页面测试。

### Step 2 验证记录

- 函数级冒烟通过：
  - `run_financial_agent("贵州茅台")` 返回 3 个步骤，且每步带有 `tool_name`
  - `tool_calls` 返回 `build_company_snapshot` 与 `analyze_company` 成功记录
  - `evaluation.quality_score` 返回 `100`
  - `ai_status` 返回 `真实 API：glm-4-flash`
- 编译检查存在非阻塞问题：
  - `python -m compileall app.py src` 时，`src/__pycache__` 出现 `WinError 5` 权限拒绝
  - 结论：属于缓存文件锁定问题，不影响功能运行与页面测试

### Step 2 结论

- Step 2 已达到可测试状态，可通知用户进行页面验证。

## 本次追加（Step 3）

### 13:30 - 13:55 执行动作

- 新增“财报对比”专用分支，支持问题中识别两个公司并生成对比结果。
- 扩展 `AgentResult` 增加 `comparison` 字段，承载对比结构化结果。
- 前端新增双公司对比结论展示区（领先方 + 理由 + 指标对比表）。
- 新增 Step 3 阶段说明文档 `docs/step-03-compare-flow.md`。

产出：

- Agent 从单公司分析扩展到双公司对比流程，且保留原链路兼容。

决策与理由：

- 对比场景先采用规则提取目标，保证可控性和可解释性；复杂语义留到后续迭代。

### Step 3 验证记录

- 对比场景通过：
  - `run_financial_agent("宁德时代和比亚迪谁更好")`
  - `intent=财报对比`
  - `comparison=True`
  - 返回双目标步骤与对比结论
- 单公司回归通过：
  - `run_financial_agent("贵州茅台")` 仍走单公司路径，`comparison=None`
- 失败场景通过：
  - `run_financial_agent("A和B谁更好")` 返回“需要两个可识别公司”的明确提示，不崩溃

### Step 3 结论

- Step 3 已达到可测试状态，可通知用户进行页面验证。

## 本次追加（Step 4）

### 14:05 - 14:20 执行动作

- 扩展对比结果结构，新增可信度分数、行业语境、限制说明和追问建议。
- 前端对比卡片增加“结论可信度”和“行业语境”。
- 新增“对比限制说明”和“建议继续追问”展示区。
- 新增 Step 4 文档 `docs/step-04-compare-confidence-followups.md`。

产出：

- 对比结论从静态结果升级为“结论 + 可信度 + 行动建议”闭环展示。

决策与理由：

- 采用规则可解释评分，不引入黑盒评估，便于求职演示时说明依据。

### Step 4 验证记录

- 对比场景验证通过：
  - `run_financial_agent("宁德时代和比亚迪谁更好")`
  - `comparison=True`
  - `confidence_score=82`
  - `industry_context` 有值
  - `followups` 返回 3 条
- 单公司回归通过：
  - `run_financial_agent("贵州茅台")` 仍为 `comparison=None`

### Step 4 结论

- Step 4 已达到可测试状态，可通知用户页面验证新增模块。

## 本次追加（Step 5）

### 14:30 - 14:45 执行动作

- 将“建议继续追问”升级为可点击按钮交互。
- 引入 `session_state` 实现追问文本自动回填与自动执行。
- 新增 Step 5 文档 `docs/step-05-clickable-followups.md`。

产出：

- 对比分析后的追问流程从手动输入升级为一键触发，提高连续交互效率。

决策与理由：

- 采用 `session_state` 最小改动实现，不破坏现有提交表单结构。

### Step 5 验证记录

- 编译通过：`python -m compileall app.py src`
- 对比结果仍返回追问建议：`followups=3`
- 单公司与对比主流程未回归

### Step 5 结论

- Step 5 已达到可测试状态，可通知用户进行页面交互测试。

## 本次追加（Step 5 修复）

### 14:55 - 15:05 执行动作

- 修复“继续问”按钮回填不生效问题。
- 调整状态流转顺序：
  - 按钮点击时只写入 `pending_followup`
  - 页面顶部在输入框实例化前读取 `pending_followup`，再写入 `query_input` 与 `auto_run_query`
- 保留自动执行逻辑，避免用户二次点击提交。

产出：

- 追问按钮触发后，输入框可稳定自动回填并自动分析。

决策与理由：

- 规避 Streamlit 对“组件实例化后再改同键状态”的限制，改为前置写入，交互更稳定。

### 修复验证

- 编译通过：`python -m compileall app.py`

## 本次追加（Step 5 二次修复）

### 15:15 - 15:30 执行动作

- 修复“继续问”点击后未回填、未自动执行问题。
- 增加追问按钮回调函数 `_queue_followup`，通过 `on_click` 可靠写入待处理追问。
- 增加 `last_agent_result` 持久化，重跑时继续渲染结果区，避免按钮事件因组件消失而丢失。
- 将渲染逻辑拆为 `render_agent_result`，统一用于手动提交与自动追问执行后的展示。

产出：

- 追问点击事件可在重跑后稳定生效，且页面可持续显示上次结果。

决策与理由：

- Streamlit 全量重跑机制下，交互按钮必须依赖稳定状态与持久化结果，避免条件渲染导致事件丢失。

## 本次追加（Step 6）

### 15:40 - 15:55 执行动作

- 新增一键冒烟回归脚本 `scripts/smoke_check.py`。
- 覆盖四类检查：健康接口、单公司、双公司、对比失败提示。
- 输出结构化 JSON 摘要，支持 CI 风格退出码（通过=0，失败=1）。
- 新增 Step 6 文档 `docs/step-06-smoke-check.md`。

产出：

- 后续每次改动后可一键验证主链路，减少手工回归成本。

决策与理由：

- 先做轻量冒烟，不引入测试框架，降低维护门槛并便于本地快速执行。

## 本次追加（阶段 A - 投递总览）

### 16:05 - 16:20 执行动作

- 新增投递版总览文档 `docs/job-delivery-overview.md`。
- 汇总 Step1-6 的目标、方案演进、架构能力、验证结果、风险边界和后续计划。
- 增加 2 分钟面试讲述提纲，便于快速口头表达项目价值。

产出：

- 项目从“开发记录”升级为“可投递讲述材料”，可直接用于简历和面试展示。

决策与理由：

- 文档按“业务问题 -> 方案 -> 结果 -> 下一步”组织，降低面试沟通成本。

## 本次追加（Step 7）

### 16:35 - 16:55 执行动作

- 新增“通用问题模式”，支持未提到具体公司的问题继续输出结果。
- 在 `ai_client.py` 增加 `analyze_general_question`（真实 API + 本地兜底双路径）。
- 在 `agent.py` 增加识别失败后的通用解读分支，不再直接返回失败页。
- 在 `app.py` 增加 `general_guidance` 数据来源标签。
- 新增 Step 7 文档 `docs/step-07-general-question-mode.md`。

产出：

- 用户即使不输入具体公司，也能获得结构化、可执行的财报方法论建议。

决策与理由：

- 优先解决用户最常见“泛化提问”断链问题，提升产品容错与对话连续性。

### Step 7 验证记录

- 通用问题验证通过：
  - `run_financial_agent("现在整体市场风险怎么看")`
  - `snapshot.found=True`
  - `company_name=通用问题模式`
  - `intent=通用财报问答`
  - `ai_status=真实 API：glm-4-flash（通用问题模式）`
- 单公司回归通过：`run_financial_agent("贵州茅台")`
- 双公司回归通过：`run_financial_agent("宁德时代和比亚迪谁更好")`

## 本次追加（Step 8）

### 17:05 - 17:20 执行动作

- 新增统一追问建议机制，覆盖通用问答、单公司、双公司和失败引导场景。
- 在 `AgentResult` 增加 `suggested_questions` 字段。
- 新增前端“你可以继续这样问”展示区，支持一键 `追问`。
- 新增 Step 8 文档 `docs/step-08-unified-followup-suggestions.md`。

产出：

- 用户任意提问后都能获得下一步问题建议，降低断链概率并增强连续交互。

决策与理由：

- 将追问建议上收至 Agent 统一产出，前端统一消费，避免各场景逻辑分散。

### Step 8 验证记录

- 编译通过：`python -m compileall app.py src`
- 三类场景建议输出通过：
  - 通用问题 `suggested_questions=3`
  - 单公司问题 `suggested_questions=3`
  - 双公司问题 `suggested_questions=3`

## 本次追加（Step 9）

### 17:55 - 18:20 执行动作

- 新增会话上下文记忆能力，支持多轮追问自动补全。
- 在 `AgentResult` 增加 `resolved_query/context_used/context_company_*` 字段。
- `app.py` 新增上下文改写器，支持“它/这家公司/刚才那个”等代词补全。
- 新增上下文状态条与“清空上下文”按钮。
- 新增 Step 9 文档 `docs/step-09-conversation-memory.md`。

产出：

- 用户连续追问时可继承上一轮公司上下文，显著降低“问题断档”。

决策与理由：

- 记忆逻辑放在前端会话层，Agent 继续聚焦分析逻辑，改动面更小、回归风险更低。

### Step 9 验证记录

- 编译通过：`python -m compileall app.py src`
- 连续追问验证通过：
  - 原始问题：`它的现金流怎么样`
  - 补全后：`贵州茅台的现金流怎么样`
  - `context_used=True`
  - 命中公司：`贵州茅台`

## 本次追加（Step 10）

### 18:30 - 18:45 执行动作

- 新增追问场景精简回答模式：基于上文追问时只展示相关文字表达。
- 增加 `render_concise_answer`，保留白话解读、进一步说明、风险提示与追问建议。
- 在渲染入口增加 `concise_only` 开关，自动追问和上下文补全追问默认启用。
- 新增 Step 10 文档 `docs/step-10-followup-concise-answer.md`。

产出：

- 多轮追问时信息密度更高、干扰更少，回答更聚焦当前问题。

决策与理由：

- 追问场景优先文本直达，不重复展示全量图表和面板，提升连续对话效率。

## 本次追加（文档整理）

### 17:35 - 17:45 执行动作

- 新增步骤合集文档 `docs/step-handbook.md`。
- 将 Step1-Step8 统一整理为同一份执行手册，按“目标/改动/验收点/原文档链接”结构输出。

产出：

- 后续查阅与面试展示时可一页定位所有阶段，减少跨文件跳转成本。

## 本次追加（Step 11）

### 18:55 - 19:10 执行动作

- 按需求移除“追问问题”功能，删除按钮与自动追问状态链路。
- 清理 `_queue_followup`、`pending_followup`、`auto_run_query` 相关逻辑。
- 保留建议文本展示，不再自动回填和自动执行。
- 新增 Step 11 文档 `docs/step-11-followup-feature-removal.md`。

产出：

- 交互逻辑简化为“用户手动发问 -> 系统分析输出”，行为更可控。

决策与理由：

- 当前阶段优先稳定主流程，减少自动交互链路带来的状态复杂度。

### Step 11 验证记录

- 代码检索确认无残留：`pending_followup/auto_run_query/_queue_followup` 均已移除
- 编译通过：`python -m compileall app.py`

## 本次追加（Step 12）

### 19:20 - 19:40 执行动作

- 新增标准上下文存储模块 `src/context_store.py`，定义 `ConversationContext`。
- 将 app 侧上下文逻辑从分散 `memory_*` 键迁移到统一 `conversation_context`。
- 增加旧键兼容迁移逻辑，首次运行自动转存。
- 更新上下文读取、清空、状态条展示和查询前后更新逻辑。
- 新增 Step 12 文档并更新步骤手册到 Step 12。

产出：

- 上下文状态管理从“分散键值”升级为“结构化对象”，扩展与维护成本显著下降。

决策与理由：

- 先做数据结构标准化，再做意图/重写策略增强，能避免后续多处重复改动。

## 本次追加（Step 13）

### 19:50 - 20:05 执行动作

- 修复表单提交后上下文栏不实时更新问题，改为“提交后写状态并立即 rerun”。
- 统一结果渲染到次轮执行，减少当轮状态与渲染交错导致的前端异常风险。
- 增加 `last_render_concise_only` 状态，保证次轮渲染模式一致。
- 新增 Step 13 文档并更新步骤手册。

产出：

- 提交后顶部上下文与结果区同步刷新，展示一致性更好。

决策与理由：

- 使用 Streamlit 官方推荐的提交后二次重跑模式，稳定性优先。

### Step 13 验证记录

- 编译通过：`python -m compileall app.py`
- 冒烟通过：`scripts/smoke_check.py --skip-health` 3/3

## 本次追加（Step 14）

### 20:15 - 20:30 执行动作

- 修复顶部上下文栏提交后不实时更新问题。
- 使用顶部容器占位方案：先声明容器，提交后再渲染上下文栏。
- 新增 Step 14 文档，并更新步骤手册到 Step 14。

产出：

- 上下文栏显示位置保持顶部，同时可反映本轮最新状态。

决策与理由：

- 相比依赖多次 rerun，容器占位能更直接地保证显示位置与状态时序一致。

### Step 14 验证记录

- 编译通过：`python -m compileall app.py`
- 冒烟通过：`scripts/smoke_check.py --skip-health` 3/3

## 本次追加（Step 15）

### 20:40 - 20:55 执行动作

- 针对前端 DOM 异常风险，改造顶部上下文栏渲染方式。
- 将上下文栏从自定义 HTML 卡片改为 Streamlit 原生组件（`st.write` + `st.button`）。
- 重启本地服务并验证健康状态。
- 新增 Step 15 文档并更新步骤手册。

产出：

- 页面渲染稳定性提升，顶部上下文栏显示路径更可靠。

决策与理由：

- 原生组件渲染在频繁 rerun 场景下更稳定，避免复杂 DOM 结构带来的移除节点异常。

### Step 15 验证记录

- 编译通过：`python -m compileall app.py`
- 服务健康：`/_stcore/health = ok`

## 本次追加（Step 16）

### 21:05 - 21:20 执行动作

- 针对“页面无法输出内容”问题，新增动态结果区原生渲染兜底方案。
- 将 Agent trace、对比结论、建议列表、追问模式回答改为 Streamlit 原生组件输出。
- 保留核心信息，不依赖复杂 HTML 节点树。
- 新增 Step 16 文档并更新步骤手册。

产出：

- 页面结果输出稳定性提升，优先恢复可用。

决策与理由：

- 在已出现前端 DOM 异常情况下，先保证稳定输出，再考虑样式增强。

### Step 16 验证记录

- 编译通过：`python -m compileall app.py`
- 冒烟通过：`scripts/smoke_check.py --skip-health` 3/3

## 本次追加（Step 17）

### 21:35 - 21:50 执行动作

- 修复“页面无法正常运行”的会话缓存兼容问题。
- 新增 `last_agent_result` 结构兼容校验。
- 对旧缓存自动清理并提示恢复。
- 渲染阶段增加异常保护，发生异常时自动回退到可提交状态。
- 新增 Step 17 文档并更新步骤手册。

产出：

- 即使存在旧会话脏缓存，页面也可自愈恢复，不再整页卡死。

决策与理由：

- 当前属于高优先级可用性问题，自愈机制比手动清缓存更可控。

### Step 17 验证记录

- 编译通过：`python -m compileall app.py`
- 服务健康：`/_stcore/health = ok`

## 本次追加（Step 18）

### 22:10 - 22:45 执行动作

- 基于用户“逐步恢复功能”要求，先确认当前仓库状态与回退后核心可运行状态。
- 识别到 `src/agent.py` 已存在，但 `src/models.py` 缺失 Agent 数据结构，导致主入口无法直接恢复。
- 在 `src/models.py` 新增：
  - `AgentStep`
  - `ToolCallResult`
  - `EvaluationResult`
  - `AgentResult`
- 在 `src/ai_client.py` 新增 `analyze_general_question(user_question)`，用于“未命中具体公司”的兜底输出。
- 由于旧 `app.py` 存在明显编码污染与局部 patch 不稳定，重建 `app.py`：
  - 接入 `run_financial_agent` 统一入口
  - 增加上下文条（当前公司 / 最近对比对）
  - 增加代理执行流程展示
  - 增加延申问题建议展示（本轮为只读）
  - 保留核心分析输出（解读、展望、评分、雷达图、日线、财务表）
- 新增阶段文档：`docs/step-18-incremental-restore-baseline.md`
- 更新总手册：`docs/step-handbook.md`

产出：

- 从“仅可运行”提升到“可解释、可观察、可持续增量恢复”的状态。
- 单公司、对比问题、通用问题三类路径均有统一 Agent 执行链路。

决策与理由：

- 先追求稳定闭环，再恢复复杂交互，避免再次出现 DOM/rerun 级故障。
- 用重建主入口替代碎片修补，降低后续维护成本和回归风险。

### Step 18 验证记录

- 编译通过：`python -m compileall app.py src`
- 服务健康：`/_stcore/health = ok`

## 本次追加（Step 19）

### 22:50 - 23:10 执行动作

- 收到用户反馈：页面有显示，但“上下文不理解”、且“通用解读不稳定”。
- 定位问题一：`run_financial_agent` 没有自动把上文公司注入追问。
- 定位问题二：`app.py` 会把“通用问题模式/N/A”写回上下文，导致后续上下文污染。
- 在 `src/agent.py` 增加上下文改写函数：
  - `_has_explicit_target`
  - `_apply_context_to_query`
  - 无显式目标时自动补齐上文公司，标记 `context_used`。
- 在 `app.py` 修复上下文回写：
  - 当 `company_name=通用问题模式` 或 `stock_code=N/A` 时，不覆盖真实上下文。
- 新增文档：`docs/step-19-context-and-general-mode-fix.md`

产出：

- 追问链路可继承上文公司，通用模式不再污染上下文。

决策与理由：

- 优先修复“上下文正确性”而非增加新交互，先保证答复稳定可信。

### Step 19 验证记录

- 编译通过：`python -m compileall app.py src`
- 脚本验证：
  - `600519 -> 它的现金流怎么样`：`context_used=True` 且公司识别为贵州茅台
  - `怎么看现金流和利润的关系`：进入通用问答并返回结果

## 本次追加（Step 20）

### 23:15 - 23:30 执行动作

- 用户要求“参考之前版本 UI，当前 UI 换成一致风格”。
- 重建 `app.py` 样式层，恢复历史卡片化风格：
  - hero 顶部说明区
  - `custom-card` 卡片容器
  - 渐变主按钮
  - 指标与内容分栏布局
- 保留现有功能链路不变：
  - `run_financial_agent` 主入口
  - 上下文栏、代理执行流程、延申问题建议
  - 通用问题模式与上下文修复逻辑
- 新增文档：`docs/step-20-ui-restore-consistency.md`

产出：

- 页面视觉回归到历史一致风格，同时保持当前功能可用。

决策与理由：

- 仅替换 UI 层，不回退功能层，避免再次引入上下文与可用性回归。

### Step 20 验证记录

- 编译通过：`python -m compileall app.py src`
- 服务健康：`/_stcore/health = ok`

## 本次追加（Step 21）

### 23:35 - 23:45 执行动作

- 按用户要求处理两项UI调整：
  1) 恢复K线图到历史样式；
  2) 调换上下文框与问题输入框的上下顺序。
- 在 `app.py::_build_price_chart` 中恢复：
  - 蜡烛图红涨绿跌配色
  - MA5/MA10/MA20 均线
  - 统一图例与坐标样式
- 在 `app.py::main` 中调整渲染顺序：
  - 先渲染问题表单
  - 再渲染上下文框
- 新增文档：`docs/step-21-kline-ui-and-layout-order.md`

产出：

- K线图视觉和信息密度回归到之前版本；输入区与上下文区顺序符合最新交互要求。

### Step 21 验证记录

- 编译通过：`python -m compileall app.py src`
- 服务健康：`/_stcore/health = ok`

## 本次追加（Step 22）

### 23:55 - 00:20 执行动作

- 用户反馈：虽然能触发双公司对比，但下方内容仍是单公司风格，不满足对比阅读需求。
- 在 `app.py` 重建并增强对比分支渲染：
  - 新增 `_render_compare_view(comparison)`。
  - 对比模式展示两家公司并排K线图（近90天，含 MA5/10/20）。
  - 使用 `comparison.rows` 输出双公司财务指标对比表。
  - 新增“文字点评（对比）”，明确领先方、原因、可信度与限制项。
- 新增 `_snapshot_for_compare_company(entry)`：
  - 根据对比对象代码/名称补抓快照，保证两边图表和公司名可独立渲染。
- 保持原有单公司分析渲染路径不变，避免功能回归。
- 新增文档：`docs/step-22-compare-visualization-upgrade.md`。

产出：

- 对比结果从“只识别意图”升级为“图表 + 表格 + 文字点评”的完整对比阅读页。

### Step 22 验证记录

- 编译通过：`python -m compileall app.py src`
- Agent 调用验证：`600519和300750谁更好` 返回 `comparison=True` 且 `rows=4`
- 服务健康：`/_stcore/health = ok`
## 本次追加（Step 25）

### 01:30 - 02:00 执行动作

- 按“简历可用化”目标推进 Day 1：建立评测基线资产。
- 新增：
  - `eval/metrics.md`（指标定义）
  - `eval/testset.csv`（20条基线测试样本）
  - `scripts/eval_runner.py`（自动评测脚本）
- 运行基线评测并记录结果。

产出：

- 项目从“主观感觉可用”升级到“有客观指标可复盘”的工程状态。

### Step 25 验证记录（基线）

- total_cases: 20
- intent_accuracy: 70.00%
- route_success_rate: 85.00%
- compare_success_rate: 100.00%
- text_quality_pass_rate: 60.00%
- empty_or_error_rate: 15.00%

分桶结果：
- compare: intent 83.33%, route 100.00%, text 100.00%
- general: intent 83.33%, route 83.33%, text 0.00%
- single_company: intent 50.00%, route 75.00%, text 75.00%
## 本次追加（Step 26）

### 02:10 - 02:35 执行动作

- 按用户要求“完成后面的内容”，补齐 Day 2 / Day 3 投递资产：
  - 架构一页纸：`docs/architecture-onepager.md`
  - Demo 演示脚本：`docs/demo-script.md`
  - 面试讲稿（1分钟/3分钟）：`docs/interview-talk-track.md`
  - 简历条目（中英文）：`docs/resume-bullets.md`
  - 交付总结文档：`docs/step-26-delivery-pack-for-internship.md`

产出：

- 项目从“可实现”升级到“可讲述、可面试、可投递”的完整交付包。

### Step 26 验证记录

- 文档资产已落盘，路径可直接访问。
