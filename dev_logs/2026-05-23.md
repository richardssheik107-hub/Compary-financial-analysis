# 2026-05-23 工作日志

## 任务：公司经营研究 Agent 升级

### 资料目录与研究上下文

- 新增 `data/reports/`，作为本地年报、季报、研报摘要入口。
- 新增 `data/reports/README.md`，说明资料放置方式和当前支持格式。
- 新增 `src/research_models.py`，定义研究 Agent 的统一数据结构。
- 决策说明：先建立结构化上下文，不直接爬取研报，降低来源不稳定和版权风险。

### 本地资料加载

- 新增 `src/report_loader.py`。
- 当前支持 `.txt` 和 `.md` 文件。
- 加载逻辑会按公司名称或股票代码匹配资料。
- 排除 `README.md`，避免说明文件被误判为公司研究资料。
- 决策说明：先打通轻量本地资料链路，后续再扩展 PDF 和向量检索。

### 研究 Agent

- 新增 `src/research_agent.py`。
- 新增 `run_company_research_agent(query)`。
- 单公司输出 `single_company_research`。
- 双公司输出 `compare_companies_deep`。
- 通用经营问题输出 `general_business_qa`，避免没有具体公司时强行套用公司研究报告。
- 输出字段覆盖业务、经营表现、盈利质量、现金流质量、成长性、风险、跟踪清单、来源依据和可信度。
- 决策说明：先固定研究报告结构，让输出从“散文式回答”升级为“可复盘的公司研究框架”。

### 对比识别修复

- 修改 `src/agent.py` 的 `_has_compare_keyword`。
- 新增识别词：`谁的`、`哪家更好`、`更好`、`更值得`。
- 更新 `eval/company_aliases.csv`，补充正常 UTF-8 中文公司别名，避免 `万科` 等公司因旧别名编码问题无法命中。
- 修改 `src/research_agent.py`，当基础 Agent 落到通用问题但别名表已识别出公司代码时，自动回到公司研究分支。
- 修复样例：`贵州茅台和宁德时代谁的经营质量更好`。
- 决策说明：用户自然语言里不一定显式说“对比”，需要覆盖更常见的比较句式。

### UI 接入

- 修改 `app.py`。
- 新增 `_render_research_report(result)`。
- 单公司和双公司分析后展示“深度研究报告”。
- 保持原有 K 线、财务表、对比表和通用问题展示逻辑不变。
- 决策说明：本阶段是增量升级，不重构原 UI，避免破坏当前可运行状态。

### 评测体系

- 新增 `eval/research_testset.csv`。
- 新增 `scripts/research_eval_runner.py`。
- 评测维度包括路由、结构完整度、证据覆盖和文本质量。
- 决策说明：为后续输出质量优化建立基线，避免只凭主观感觉判断效果。

### 已完成校验

- `python -m compileall src/research_models.py src/report_loader.py src/research_agent.py app.py` 通过。
- 后端样例 `贵州茅台整体经营状况怎么样` 可以生成单公司研究报告。
- 后端样例 `贵州茅台和宁德时代谁的经营质量更好` 可以生成双公司深度对比报告。

### 待办

- 接入 PDF 解析。
- 增加真实年报、季报和研报摘要样本。
- 将摘要器升级为检索增强生成。
- 扩展研究评测集到 50 条。

---

## Step 39 - V1 可验证版（项目题目升级 + 报告导出）

### 执行动作

- 修改 `app.py`：
  - 应用 `page_title` 改为“公司经营研究 Agent”。
  - 首屏 hero 标题与副文案改为研究 Agent 定位。
  - 新增 `_build_research_markdown(result)`，统一构建研究报告 Markdown 内容。
  - 在深度研究报告区域增加 `st.download_button` 导出按钮。
- 修改 `README.md`：
  - 项目标题改为“公司经营研究 Agent”。
  - 项目定位文案同步为“公司经营研究 + 结构化输出”。
- 新增 `docs/step-39-v1-research-agent-productization.md`：
  - 记录本步目标、改动、验收标准和后续风险。

### 关键决策

- 决定先做 Markdown 导出而不是 Word：
  - 原因：改动小、验证快、不影响当前 UI 主流程，适合作为第一版可测能力。
- 决定只做题目升级和能力增量，不重构页面结构：
  - 原因：避免引入无关风险，保证当前可运行版本持续可用。

### 可定位对象

- 函数：`_build_research_markdown`
- 页面区域：`深度研究报告`
- 入口：`main()`

### 当前状态

- 已完成代码与文档更新，待编译与冒烟验证。

### Step 39.1 - RAG 与 Skill 质量增强

- 修改 `src/report_loader.py`：
  - 新增 `_query_terms(query)` 与 `retrieve_relevant_passages(documents, query, top_k)`。
  - 支持按用户问题对本地资料进行轻量片段检索，用于优先注入高相关证据。
- 新增 `src/research_skill.py`：
  - 新增 `refine_research_result(result, query)`。
  - 有 API：执行结构化润色；无 API：执行本地保守清洗；全程不影响主流程可用性。
- 修改 `src/research_agent.py`：
  - `summarize_company_sources` 接入 RAG 片段命中。
  - 单公司、双公司、通用问答输出都接入 `refine_research_result` 二次优化。

#### 决策说明

- 先做“轻量 RAG + 写作 skill”，不引入向量数据库：
  - 原因：改动范围可控、可快速验证质量收益，避免一次引入重组件影响稳定性。
- Skill 采用“可降级”设计：
  - 原因：API 异常时仍保持可输出，避免为提升文案而牺牲可用性。

### Step 40 - 资料目录统一接入与可视化验证

- 修改 `src/report_loader.py`：
  - 新增 `load_research_documents`，统一扫描 `data/reports`、`data/company_info`、`data/industry_info`。
  - 返回统一文档列表 + 命中统计（reports/company_info/industry_info/total）。
- 修改 `src/research_models.py`：
  - `CompanyResearchContext` 增加 `source_summary`。
  - `ResearchResult` 增加 `source_summary`。
- 修改 `src/research_agent.py`：
  - 研究上下文改为使用统一资料加载入口。
  - 单公司/双公司/通用问答结果都填充 `source_summary`。
- 修改 `app.py`：
  - 深度研究报告区域新增“资料命中概览”表格。
  - 导出的 Markdown 报告同步包含命中统计。

#### 决策说明

- 先做“可见可验”的命中统计，而不是直接做复杂向量检索：
  - 原因：用户能立即验证 Agent 是否真的在使用资料，降低黑盒感。

### Step 41 - 研究图表补齐（功能优先）

- 修改 `src/research_models.py`：
  - `ResearchResult` 增加 `key_metrics` 与 `compare_metrics` 字段，承载图表数据。
- 修改 `src/research_agent.py`：
  - 单公司结果填充收入/净利润/经营现金流等核心指标。
  - 双公司结果填充对比图所需指标数组。
- 修改 `app.py`：
  - 新增 `_render_research_charts(result)`。
  - 深度研究报告中新增两类图：
    - 单公司：经营质量三指标柱状图；
    - 双公司：核心指标对比分组柱状图。

#### 决策说明

- 图表先用“确定性指标绘制”，不依赖 LLM 生成代码：
  - 原因：保证稳定性，避免图表链路不确定导致页面失败。

### Step 42 - 资料命中增强与冒烟稳定性修复

- 修复 `scripts/smoke_check.py` 编码损坏问题，重写为 UTF-8 版本。
- 冒烟用例调整：
  - 单公司：`贵州茅台`
  - 双公司：`宁德时代和比亚迪谁更好`
  - 对比失败提示：`A和B谁更好`（改为关键词容错断言，避免文案细微变化导致误报失败）
- 升级 `src/report_loader.py`：
  - 支持 `.pdf` 文件加载（依赖 `pypdf`，最多提取前 30 页文本）。
  - 统一资料类型命名：`年报文本`、`研报观点`、`公司资料`、`行业资料`、`本地资料`。
  - 保留 `txt/md` 兼容，避免破坏已有资料目录。
- 更新 `requirements.txt` 增加 `pypdf>=4.2.0`。

#### 关键决策

- 先做 PDF 文本提取，不做 OCR：
  - 原因：低风险、可快速验证“资料命中不全为 0”的主诉；扫描版 PDF 作为下一步增强。
