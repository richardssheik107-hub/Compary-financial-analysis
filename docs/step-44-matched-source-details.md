# Step 44：命中资料明细展示

## 目标

让“资料命中概览”从数字变成可追踪信息，直接看到命中了哪些资料文件。

## 改动

- `src/report_loader.py`
  - 在 `source_summary` 中新增 `matched_titles`（最多前 8 个命中文件标题）。
- `app.py`
  - 研究报告区域新增“命中资料明细”展示块。
  - Markdown 导出中新增 `matched_titles` 字段。

## 验收

- 命中资料后页面能看到文件名明细。
- 无命中时显示“无”。
- 不影响单公司/双公司/通用问答流程。
