# Step 21：K线图样式恢复与输入区顺序调整

## 目标

1. 将K线图恢复到历史版本的可读样式（蜡烛 + MA均线）。
2. 调换“上下文框”和“问题输入框”的上下顺序。

## 本步改动

1. `app.py` K线图恢复
- 在 `_build_price_chart` 中恢复：
  - 蜡烛图红涨绿跌配色
  - MA5 / MA10 / MA20 三条均线
  - 更完整图例与坐标样式

2. `app.py` 布局顺序调整
- 在 `main()` 中，将问题表单放在上方；
- 将上下文框 `_render_context_bar()` 下移到问题表单之后。

## 验收标准

1. 页面中K线图显示蜡烛图+三条均线。
2. 输入框在上，上下文框在下。
3. 服务健康检查正常，功能不回归。
